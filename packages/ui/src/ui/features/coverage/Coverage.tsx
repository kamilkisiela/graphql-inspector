import React from 'react';

export default function Coverage() {
  return (
    <ul>
      <li>Whole schema</li>
      <li>Count of hits on the left</li>
      <li>Light green background if used</li>
      <li>Light red background if unused</li>
      <li>When user click on a number it should show a list of documents</li>
      <li>Document should be opened then as a modal window (probably the same logic as in validation)</li>
    </ul>
  );
}

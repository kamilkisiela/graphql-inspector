import React from 'react';

export default function Validate() {
  return (
    <ul>
      <li>list of errors</li>
      <li>Each error should show its location on a document, either through link to a page or a modal window ((probably the same logic as in coverage)</li>
    </ul>
  );
}

{
  "scripts": {
    "start": "docusaurus-start",
    "prestart": "yarn live:dev",
    "build": "docusaurus-build && yarn live:prod",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version",
    "live:dev": "rm -rf static/live && cd live && yarn build && cp -r ./build ../static/live",
    "live:prod": "rm -rf build/graphql-inspector/live && cd live && yarn && yarn build && cp -r ./build ../build/graphql-inspector/live"
  },
  "devDependencies": {
    "docusaurus": "1.13.0"
  }
}

type Post {
  id: ID
  title: String
  createdAt: String
  modifiedAt: String
}

type BlogPost {
  title: String
  createdAt: String
  id: ID
}

type Comment {
  id: ID
}

type Query {
  post: Post!
}
